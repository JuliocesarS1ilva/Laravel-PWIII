@extends('layouts.app')
@section('content')<div class="form-wrap"><p class="eyebrow">NOVO REGISTRO</p><h1>Crie uma conta</h1><p>O cadastro será salvo diretamente no banco de dados.</p><form method="POST" action="{{ route('users.store') }}">@csrf<label>Nome<input name="name" value="{{ old('name') }}" required></label><label>E-mail<input type="email" name="email" value="{{ old('email') }}" required></label><label>Senha<input type="password" name="password" required></label><label>Confirmar senha<input type="password" name="password_confirmation" required></label>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<button class="btn" type="submit">Salvar conta →</button></form></div>@endsection
