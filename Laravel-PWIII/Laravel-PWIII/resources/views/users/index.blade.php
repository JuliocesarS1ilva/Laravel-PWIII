@extends('layouts.app')
@section('content')<section class="page-head"><div><p class="eyebrow">GERENCIAMENTO</p><h1>Contas cadastradas</h1><p>{{ $users->count() }} usuários disponíveis na base atual.</p></div><a class="btn" href="{{ route('users.create') }}">+ Nova conta</a></section>@if(session('success'))<div class="success">{{ session('success') }}</div>@endif<div class="table-card"><table><thead><tr><th>Usuário</th><th>E-mail</th><th>Cadastrado em</th></tr></thead><tbody>@forelse($users as $user)<tr><td><div class="avatar">{{ strtoupper(substr($user->name,0,1)) }}</div>{{ $user->name }}</td><td>{{ $user->email }}</td><td>{{ optional($user->created_at)->format('d/m/Y') }}</td></tr>@empty<tr><td colspan="3">Nenhum usuário encontrado.</td></tr>@endforelse</tbody></table></div>@endsection
